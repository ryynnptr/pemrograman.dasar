Program Menghitung_jumlah_digit_sebuah_bilangan_yang_dimasukan_pengguna

mulai
var bilangan : integer;

bilangan <- [123];
jumlah_digit <- [0];

while = bilangan;
while > 0;
jumlah_digit = jumlah_digit + (bilangan) then;
jumlah_digit = jumlah_digit;
end while;

read "Nilai Jumlah Digit: ", jumlah_digit;
end;
